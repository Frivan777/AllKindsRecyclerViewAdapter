<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.frivan.tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".view.activities.animation.share.image.ImageActivity"
            android:theme="@style/AppTheme.ImageActivity" />
        <activity android:name=".view.activities.animation.share.image.ShareImageActivity" />
        <activity
            android:name=".view.activities.animation.share.detail.DetailContactActivity"
            android:theme="@style/AppTheme.DetailContactActivity" />
        <activity
            android:name=".view.activities.animation.share.list.ContactListActivity"
            android:theme="@style/AppTheme.ContactListActivity" />
        <activity
            android:name=".view.activities.animation.main.AnimationActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".view.activities.main.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>
